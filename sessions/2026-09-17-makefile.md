# Session 2026-09-17: Makefile build commands

Added `make build-debug` (also the default), `build-release`, matching run
targets, workspace tests, Clippy, plugin install and type check, an aggregate
`check`, and `clean`. Each Rust profile first builds `libghostty-vt-sys` with
the existing script. The Cargo configuration keeps the Zig engine at
`ReleaseFast` in both profiles; the Rust dev profile still retains debug
information and optimizes the rendering path.

On this Fedora host, both full builds and all 20 workspace tests passed using
a temporary linker search path for the installed `libxkbcommon-x11.so.0`.
A normal clean build needs the `libxkbcommon-x11-devel` package, which supplies
the unversioned linker library. `make plugins-check` passed. `make clippy`
could not run because this host's Cargo installation has no Clippy subcommand.
